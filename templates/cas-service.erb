              <bean class="net.shibboleth.idp.cas.service.ServiceDefinition"
                    c:regex="<%= scope['regex'] %>"
                    p:group="<%= scope['title'] %>"
                    p:authorizedToProxy="<%= scope['proxy'] %>"
                    p:singleLogoutParticipant="<%= scope['single_logout'] %>" />
