              <bean class="net.shibboleth.idp.cas.service.ServiceDefinition"
                    c:regex="<%= @regex %>"
                    p:group="<%= @name %>"
                    p:authorizedToProxy="<%= @proxy %>"
                    p:singleLogoutParticipant="<%= @single_logout %>" />
